# SECURITY

- No write credentials permitted
- Read-only scopes only
- No secrets in git (except .env.example)
- All API routes must be GET-only
- Report vulnerabilities via private channel
- Any violation must be escalated and reverted
